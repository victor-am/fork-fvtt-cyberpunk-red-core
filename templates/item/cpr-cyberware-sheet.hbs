<form>
  {{!-- Item Header Navigation --}}
  {{> "systems/cyberpunk-red-core/templates/item/parts/cpr-header.hbs"}}
  {{!-- Item Sheet Navigation --}}
  <nav class="tabs" data-group="primary">
    <a class="" data-tab="description">DESCRIPTION</a>
    <a class="" data-tab="details">DETAILS</a>
  </nav>
  <section class="content">
    {{!-- Inventory Tab --}}
    <div class="tab" data-group="primary" data-tab="description">
      {{> "systems/cyberpunk-red-core/templates/item/parts/cpr-info-pane-skill.hbs"}}
      {{> "systems/cyberpunk-red-core/templates/item/parts/cpr-description.hbs"}}
    </div>
    <div class="tab" data-group="primary" data-tab="details">
      <section class="">
        {{!-- MARKET PRICE --}}
        <div>
          Price: <input name="data.price.market" class="" type="text" value="{{data.price.market}}">
        </div>
        {{!-- MARKET PRICE --}}
        <div>
          Static Humanity Loss: <input name="data.humanityLoss.static" class="" type="text" value="{{data.humanityLoss.static}}">
        </div>
        {{!-- MARKET PRICE --}}
        <div>
          Rolled Humanity Loss: <input name="data.humanityLoss.roll" class="" type="text" value="{{data.humanityLoss.roll}}">
        </div>
        {{!-- IS THIS FOUNDATIONAL CYBERWARE --}}
        <div>
          <label class="label-text">Foundational</label>
          {{#if data.isFoundational}}
          <a class="item-checkbox" data-target="data.isFoundational"><i class="far fa-check-circle"></i></a>
          {{!-- OPTIONAL SLOTS --}}
          <div>
            Optional Slots: <input name="data.optionSlots" class="" type="text" value="{{data.optionSlots}}">
          </div>
          {{else}}
          <a class="item-checkbox" data-target="data.isFoundational"><i class="far fa-circle"></i></a>
          {{/if}}
        </div>
        {{!-- WHAT INSTALL LOCATION IS REQUIRED --}}
        <div>
          Install:
          <select name="data.install" data-dtype="{{data.install}}">
            {{#select data.install}}
            {{#each (findConfigObj "cyberwareInstallList") as |install i|}}
            <option value="{{i}}">{{localize install}}</option>
            {{/each}}
            {{/select}}
          </select>
        </div>
        {{!-- WHAT TYPE OF CYBERWARE IS THIS --}}
        <div>
          Type:
          <select name="data.type" data-dtype="{{data.type}}">
            {{#select data.type}}
            {{#each (findConfigObj "cyberwareTypeList") as |type t|}}
            <option value="{{t}}">{{localize type}}</option>
            {{/each}}
            {{/select}}
          </select>
        </div>
      </section>
    </div>
  </section>
</form>