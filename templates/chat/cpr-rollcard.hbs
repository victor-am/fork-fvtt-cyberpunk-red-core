{{log "CPR-ROLLCARD.HBS"}}
{{!-- If this is a damage roll --}}
{{#if (eq rollType "damage")}}
<div class="white-bg" data-roll-type="{{rollType}}">
  <div class="cpr-block horizontal half-height chat-rollTitle-{{rollType}}">
    <p class="center-text width-grow largest-text">{{rollTitle}}</p>
  </div>
  <div class="rollcard-body cpr-block rollcard">
    <span class="massive-text">{{resultTotal}}{{#if wasCritical}}!{{/if}}</span>

    {{!-- display FA dice from array --}}
    {{#each diceResults as |die|}}
    {{log die}}
    {{#if (eq die 1)}}
    <i class="fas fa-dice-one"></i>
    {{/if}}
    {{#if (eq die 2)}}
    <i class="fas fa-dice-two"></i>
    {{/if}}
    {{#if (eq die 3)}}
    <i class="fas fa-dice-three"></i>
    {{/if}}
    {{#if (eq die 4)}}
    <i class="fas fa-dice-four"></i>
    {{/if}}
    {{#if (eq die 5)}}
    <i class="fas fa-dice-five"></i>
    {{/if}}
    {{#if (eq die 6)}}
    <i class="fas fa-dice-six"></i>
    {{/if}}
    {{/each}}

    {{#if wasCritical}}
    <span>Roll: {{diceTotal}}</span>
    <span>Criticals: {{crits}}</span>
    <span>Bonus Damage: {{bonusDamage}}</span>
    {{else}}
    <span>Roll: {{diceTotal}}</span>
    {{/if}}

    {{#if (gt mods 0)}}
    <span>Mods: {{mods}}</span>
    {{/if}}

    <span class="chat-rollTotal">Result Total: {{resultTotal}}</span>

    <span>Location: {{location}}</span>
  </div>
    
</div>
{{!-- And if it's any other kind of roll --}}
{{else}}
<div class="chat-rollCard white-bg" data-roll-type="{{rollType}}">
  <h2 class="chat-rollTitle-{{rollType}}">{{rollTitle}}</h2>
  <section>
    <table>
      <tr>
        <td>
          <h3>{{resultTotal}}</h3>
        </td>
        <td>
          {{#if wasCritical}}
          Roll: {{initialRoll}} <br>
          Critical Roll: {{criticalRoll}} <br>
          <div class="chat-rollTotal">Total: {{rollTotal}}</div> <br>
          {{else}}
          Roll: {{initialRoll}} <br>
          {{/if}}

          {{#if mods}}
          Roll Modifiers: {{mods}} <br>
          <div class="chat-rollTotal">Mods Total: {{modsTotal}}</div><br>
          {{/if}}
          {{#if (gt roleValue 0)}}
          Role Ability: {{roleValue}} <br>
          {{else}}
          Stat Level: {{statValue}} <br>
          Skill Level: {{skillValue}} <br>
          {{/if}}
          <div class="chat-rollTotal">Result Total: {{resultTotal}}</div><br>
        </td>

      </tr>
    </table>
  </section>
</div>
{{/if}}
{{log "END CPR-ROLLCARD.HBS"}}