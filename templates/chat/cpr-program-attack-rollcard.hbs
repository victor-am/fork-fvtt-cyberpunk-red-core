{{trace "CPR-PROGRAM-ATTACK-ROLLCARD.HBS"}}
<div class="rollcard">
  <div class="rollcard-top">
    <div class="cpr-block chat-rollTitle-stat">
      <div class="text-center text-padding-top text-normal">{{rollTitle}}</div>
      <div class="rollcard-subtitle">
        <div class="rollcard-subtitle-center text-small">{{localize "CPR.rolls.attack"}}</div>
        <div class="rollcard-subtitle-right">
        </div>
        <div class="rollcard-subtitle-2-center text-small">{{#if (compare rollCardExtraArgs.program.data.class "===" "blackice")}}{{localize (findConfigValue "blackIceType" rollCardExtraArgs.program.data.blackIceType)}} {{/if}}{{localize (findConfigValue "programClassList" rollCardExtraArgs.program.data.class)}}</div>
      </div>
    </div>
  </div>
  <div class="rollcard-bottom">
    <div class="cpr-block">
      <div class="d10-rollcard-data">
        <div class="d10-dice-div">
          {{!-- die image for initialRoll --}}
          {{#if criticalCard}}
          {{#if wasCritSuccess}}
          <img class="d10" src="systems/cyberpunk-red-core/icons/dice/red/d10_{{initialRoll}}_preem.svg">
          <i class="fas fa-plus"></i>
          {{else}}
          <img class="d10" src="systems/cyberpunk-red-core/icons/dice/red/d10_{{initialRoll}}_fail.svg">
          <i class="fas fa-minus"></i>
          {{/if}}
          <img class="d10" src="systems/cyberpunk-red-core/icons/dice/red/d10_{{math "abs" criticalRoll}}.svg">
          {{else}}
          <img class="d10" src="systems/cyberpunk-red-core/icons/dice/black/d10_{{initialRoll}}.svg">
          {{/if}}
        </div>
        <div class="d10-number-div">
          <a><span class="clickable{{#if criticalCard}} fg-red text-bold{{/if}}" data-action="toggleVisibility"
              data-visible-element="d10-data-details">{{resultTotal}}</span></a>
        </div>
        <div class="d10-data-div">
          <div class="d10-data-details hide">
            <div class="text-normal text-bold">{{localize statName}} + {{statValue}}</div>
            <div class="text-normal text-bold">{{localize skillName}} + {{skillValue}}</div>
            {{#if mods}}
            <span class="text-normal text-nowrap">{{localize "CPR.rolls.rollMods"}}: {{mods}}</span>
            <div class="text-normal text-bold text-nowrap">{{localize "CPR.rolls.mods"}} {{localize "CPR.rolls.total"}}:
              {{totalMods}}</div>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{{trace "END CPR-PROGRAM-ATTACK-ROLLCARD.HBS"}}
