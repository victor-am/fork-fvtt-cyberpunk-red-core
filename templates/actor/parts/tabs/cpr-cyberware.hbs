{{log "CPR-CYBERWARE-HBS"}}
<div class="dynamic-list cyberware">
    {{log this}}
    {{#each (findConfigObj "cyberwareTypeList") as |cyberware c|}}
    <div class="row header black-bg white-fg bold inventory">
        <span class="section name width-quarter" style="flex: 0 0 150px">{{localize cyberware}}</span>
        <span class="section data width-grow">{{localize "CPR.data"}}</span>
        <span class="section action width-quarter right-text">{{localize "CPR.actions"}}</span>
    </div>
    {{!-- Revist...? --}}
    {{#if (true)}}
        {{>generatePartial "systems/cyberpunk-red-core/templates/actor/parts/common/inventory/content/cpr-cyberware-content.hbs"}}
    {{/if}}
    {{/each}}
</div>
{{log "END CPR-CYBERWARE-HBS"}}